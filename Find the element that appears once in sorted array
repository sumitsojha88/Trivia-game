#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main()
 {
	int n,t,i;
	cin>>t;
	while(t--)
	{
	    int a[n];
	    cin>>n;
	    for(int i=0;i<n;i++)
	    {
	        cin>>a[i];
	        
	    }
	    sort(a,a+n);
	    for(int i=0;i<n;i++){
	    if(a[i]!=a[i+1] && a[i]!=a[i-1])
	    cout<<a[i]<<endl;
	    }
	    
	}
	return 0;
}
